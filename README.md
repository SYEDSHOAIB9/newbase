cd  
git init  
git clone “repo”   } clone
ls  
cd  
ls  
git init  
git pull “repo”    } pull
ls  
echo “f1 file” > f1.txt  
git add .  
git commit -m “hello world”  
git branch  
git push origin master  
git remote -v  
git remote add origin  
git remote set-url origin “token & repo”  
git push origin master
{program4}
cd  
git init  
git clone “repo”   } clone
ls  
cd  
ls  
git init  
git pull “repo”    } pull
ls  
echo “f1 file” > f1.txt  
git add .  
git commit -m “hello world”  
git branch  
git push origin master  
git remote -v  
git remote add origin  
git remote set-url origin “token & repo”  
git push origin master
{ program5}
cd  
git branch  
git log  
git checkout -b feature  
git log  
git branch  
echo “f1 added” > f1.txt  
echo “f2 added” > f2.txt  
git add .  
git commit -m “features added”  
git log  
git checkout main  
git log  
git rebase feature  
git remote set-url origin “token & repo”  
git push origin main
}
